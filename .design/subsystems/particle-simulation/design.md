---
id: particle-simulation
type: subsystem
title: particle-simulation
description: 解析取樣、力場積分、SoA 緩衝與固定時步推進
status: active
created: 2026-08-19
updated: 2026-08-19
parent: system
related-adr: [adr-0001, adr-0006, adr-0010, adr-0017, adr-0018]
---

# 粒子模擬 子系統架構

## 定位與範圍

承接 [system.md](../../system.md)「子系統劃分 → particle-simulation」。住**純核心**（唯一例外是固定時步規劃器，見下），回答的問題是「**這一幀長什麼樣**」——拿 [magic-semantics](../magic-semantics/design.md) 編出來的法術，加上一個時間，算出一整塊粒子緩衝。

它是混合粒子模型的實作：**解析為主，力場為輔**。多數魔法的粒子位置是時間的純函數（無狀態、可任意快轉倒帶）；需要粒子被外力拉扯時才啟用力場層，那是整個系統唯一的跨幀狀態。

**做**：SoA 粒子緩衝的表徵與不變量、解析層取樣、力場層的固定時步純狀態轉移與位移疊加、效能治理（時間窗剔除、每發射器提升、平行分片、預算）、固定時步規劃。

**明確不做**：

- **不編譯魔法陣**。編譯後的法術是輸入，怎麼來的不關本子系統的事。
- **不繪製、不投影**。輸出是抽象 3D 座標的緩衝。
- **不做粒子對粒子的互動**。力場層只支援「場對粒子」。碰撞、鄰居查詢、空間分割是主架構明列的**永久非目標**。
- **不做跨幀的緩衝重用**。這是**改判**而非疏漏：觀測回傳的緩衝是宿主可長期持有的純值，跨幀重寫同一塊會偷改上一幀。

## 對外契約（Public Interface & DTOs）

### C1 取樣

| 條目 | 契約 |
|---|---|
| **C1.1 主取樣** | （編譯後的法術、施法上下文、時間）→ 一塊粒子緩衝。純函數 |
| **C1.2 序列／平行等價** | 粒子數超過門檻時走分片平行路徑，否則走序列路徑。兩者結果**逐位元相同，且與執行緒數無關**——決定論由切分方式結構性保證，不靠碰巧 |

### C2 粒子緩衝（DTO）

九個平行的無包裝數值欄加一個計數：三軸位置、尺寸、生命、封裝顏色，以及三軸**選配**速度。

| 不變量 | 內容 |
|---|---|
| **欄位長度** | 前六欄長度皆等於計數；三個速度欄要嘛全空、要嘛全滿，且三欄同步——**絕不半滿** |
| **前六欄凍結** | 名稱、型別、順序、語意自骨架時期起逐位元未變。第三方宿主與 C ABI 都依賴這件事 |

消費者只需問一個問題（「這塊緩衝有速度嗎」）就能決定怎麼讀，不必各自防禦。

### C3 力場層

| 條目 | 契約 |
|---|---|
| **C3.1 場加速度** | （場的集合、一個位置）→ 加速度。**只吃位置**，不吃速度 |
| **C3.2 每步輸入** | （法術、上下文、時間）→ 本步各槽位的年齡與解析基準位置 |
| **C3.3 固定步積分** | （場、步長、每步輸入、上一步狀態）→ 新狀態。只作用於**施放階段**的槽位 |
| **C3.4 位移取出** | 依存活槽位的列舉順序取出累積位移 |

### C4 對齊面（解析層外露的兩個半邊）

| 條目 | 契約 |
|---|---|
| **C4.1 單粒子位置** | 算出某個發射器的某顆粒子在某個年齡的位置。力場層以此取得解析基準，兩層因此共用**同一份**位置定義而非兩份拷貝 |
| **C4.2 存活槽位列舉** | 某時刻所有存活槽位的有序列舉，**其順序恰為緩衝的列順序**。這是位移能對齊回緩衝的唯一依據 |

### C5 固定時步規劃

（步長、單幀最大步數、經過時間、累加器）→（本幀要跑幾步、新的累加器）。積壓超過上限時**截斷並丟棄餘額**：模擬變慢，而不是凍結。

### 組合點語意（本子系統對外最重要的一條約定）

```text
最終位置 = 解析位置 + 力場位移
```

力場層**不吃粒子緩衝**——緩衝的列每幀變動，不能當身分。它以編譯期固定的槽位身分為鍵，疊加時用 C4.2 的列舉對齊。

### 兩條額外的不變量

| 不變量 | 內容 |
|---|---|
| **零場免費** | 沒有力場的法術**結構性跳過**整層，成本與力場層不存在時完全相同——不是「算出零位移」，是整層不進入 |
| **平行等同** | 見 C1.2 |

## 內部模組劃分（Internal Modules）

| 模組 | 層 | 單一職責 |
|---|---|---|
| **M1 SoA 緩衝** | 核心 | 緩衝的表徵、不變量檢查、單趟填充的建構路徑（含速度欄的選配路徑） |
| **M2 解析層** | 核心 | 主取樣與分片平行、單粒子位置與存活槽位列舉、時間窗剔除、形狀取樣、速度的有限差分 |
| **M3 力場層** | 核心 | 場的求值、每槽位積分狀態、固定步積分與位移取出 |
| **M4 時步規劃** | **邊界** | 累加器與截斷護欄。**唯一不住核心的模組**：它是純函數，放邊界層是為了讓外殼主迴圈與 C ABI 消費**同一份**規劃器而非兩份拷貝 |

依賴方向：`M1 ← M2 ← M3`（力場層以解析層的位置為基準），`M4` 不依賴任何人。

## 資料流管線（Data Flow Pipeline）

```text
輸入      編譯後的法術 ＋ 施法上下文 ＋ 這一幀經過的時間
  │
驗證      無。輸入來自編譯器，型別已保證合法；
          唯一的執行期防護是 M4 的步數截斷
  │
業務處理  ┌─ M4 規劃：經過時間 → 本幀要跑幾步（固定步長）
          │
          ├─ 每一步（僅當法術帶場）：
          │    M2 算出本步各槽位的解析基準位置（C3.2）
          │    M3 以半隱式積分推進每槽位狀態（C3.3）
          │
          └─ 每一幀恰一次：
               M2 取樣（C1.1）：時間窗剔除 → 每發射器提升 →
               形狀取樣／軌跡／算式 → 分片平行 → 單趟填滿緩衝
  │
儲存      力場狀態是**系統唯一的跨幀狀態**；零場的法術完全沒有這一項
  │
輸出      粒子緩衝 ＋（若有場）依 C4.2 順序取出的位移
          兩者在 boundary-host 相加，成為最終位置
```

## 模組間公開介面（Module Interfaces）

| # | 呼叫方向 | 契約內容 |
|---|---|---|
| **I1** | M2 → M1 | 取樣以「先數後填」的方式建構緩衝：先算出確切列數，再單趟填滿，**零中介結構** |
| **I2** | M3 → M2 | 力場層向解析層要每槽位的年齡與基準位置（C3.2 的實作依據）；這是「兩層共用同一份位置定義」的接點 |
| **I3** | M2 內部 | 分片的切分只依法術與時間決定，**與執行緒數無關**——這是 C1.2 決定論的結構性保證所在 |
| **I4** | M3 → M1 | 力場狀態以攤平的無包裝欄表示。**這個表徵明文不凍結**，是它能被重寫的理由 |

## 使用的技術

沿用核心的依賴白名單。子系統特有的三項選型：

| 選型 | 理由 |
|---|---|
| **SoA ＋ 無包裝向量** | 無指標追蹤、快取友善；垃圾回收只見少數大塊而非十萬個小物件；連續記憶體可直接餵給渲染後端與 C ABI |
| **先數後填的單趟建構** | 先算出確切大小再一次填滿，零中介。取代了「先建串列再轉向量」的舊寫法 |
| **純策略式平行** | 平行取樣的唯一可接受手段：它只選擇求值順序、永不改變值，因此不破壞「核心零 IO、簽名中無效果型別」。以執行緒或不安全介面實作做不到這一點 |

## 架構圖

```text
  編譯後的法術（magic-semantics）   施法上下文    這一幀的經過時間
            │                          │               │
            │                          │               ▼
            │                          │      ┌──────────────────┐
            │                          │      │ M4 時步規劃（邊界）│
            │                          │      │  C5：跑幾步       │
            │                          │      │  截斷護欄         │
            │                          │      └────────┬─────────┘
            │                          │               │ n 個固定步
            ▼                          ▼               ▼
  ┌────────────────────────────┐          ┌──────────────────────────┐
  │ M2 解析層                   │          │ M3 力場層                 │
  │  時間窗剔除                  │          │  C3.1 場加速度（只吃位置） │
  │  每發射器提升                │──I2────▶│  C3.3 半隱式固定步積分     │
  │  形狀／軌跡／算式             │  基準    │  僅施放階段的槽位          │
  │  C1.2 分片平行（I3）         │  位置    │  跨幀狀態（唯一一處）       │
  │  C4.1 單粒子位置             │          │  C3.4 依 C4.2 序取位移    │
  │  C4.2 存活槽位列舉 ═══════════════════════▶（對齊用）              │
  └─────────────┬──────────────┘          └────────────┬─────────────┘
                │ I1 先數後填                            │
                ▼                                        │
  ┌────────────────────────────┐                        │
  │ M1 SoA 緩衝                 │                        │
  │  九欄，後三欄選配             │                        │
  │  空或滿，絕不半滿             │                        │
  └─────────────┬──────────────┘                        │
                │                                        │
                └──────────────┬─────────────────────────┘
                               ▼
                最終位置 = 解析位置 + 力場位移
                （相加發生在 boundary-host）
```

## 開發階段

對應主架構 P1（骨架）與 P2（可用的庫）的效能半場，並承擔全部量化目標。內部里程碑三個，皆已達成：

| 里程碑 | 內容 |
|---|---|
| **M1 兩層模型成立** | 解析層與力場層各自可用，且組合語意釘死 |
| **M2 達到吞吐目標** | 1 萬～10 萬粒 |
| **M3 多核與新欄** | 平行取樣、速度欄的選配加欄範式 |

**進度數字會低估這個子系統的成熟度**：M1 的緩衝與取樣器、M3 的兩項，主場都記在別的子系統（見 [legacy-map.md](../../legacy-map.md)）。緩衝與解析層幾乎每一輪都被動到，這是全專案最常見的「參與者」。

## 已交付（歷史）

下列 feature 已於舊體系交付並驗收，設計文檔留在 `docs/` 為唯讀歷史（見 [legacy-map.md](../../legacy-map.md)）。**本節不列入 `/arch-audit status` 的進度統計**，也不會被 `/subsys-build` 當成待辦——它記錄的是這個子系統已經長成什麼樣子。

| feature | 一句話說明 | 模組 | 舊文檔 |
|---------|-----------|------|--------|
| force-field-layer | 三種場、穩定槽位身分、加法位移疊加、零場結構性跳過 | M3, M2 | `docs/spec/func-0007-*.md` |
| performance-budget | 先數後填、每發射器提升、時間窗剔除、力場狀態攤平、結構化預算 | M1, M2, M3 | `docs/spec/func-0010-*.md` |

### force-field-layer

- **階段**：階段一（已交付）
- **負責模組**：M3 力場層、M2 解析層（外露對齊面）
- **實作的 Level 2 介面**：C3.1 場加速度、C3.2 每步輸入、C3.3 固定步積分、C3.4 位移取出、C4.1 單粒子位置、C4.2 存活槽位列舉；建立 I2、組合點語意與「零場免費」不變量
- **資料流管線段落**：業務處理段的「每一步」分支，以及輸出段的位移
- **當初的驗收標準**：三種場都能拉動施放粒子；槽位身分跨幀穩定（緩衝的列變動不影響對應關係）；位移能正確對齊回緩衝的列；**沒有場的法術逐位元等同力場層不存在**；重載一律以全靜止狀態起步
- **明確不做**：不讓場作用於繪製與收束階段的粒子（陣是幾何，不是被吹的煙）；不做粒子對粒子；不讓場吃速度（屬 #5）

### performance-budget

- **階段**：階段二（已交付）
- **負責模組**：M1 SoA 緩衝、M2 解析層、M3 力場層
- **實作的 Level 2 介面**：無新增條目，重寫 C1.1 的內部路徑與 C2 的建構方式；建立 I1；力場狀態改用 I4 的攤平表徵
- **資料流管線段落**：業務處理段全部——取樣與積分兩條路徑
- **當初的驗收標準**：1 萬～10 萬粒的取樣時間落在單幀預算內並有實測數字；每幀的配置次數降到與欄數同階、零中介結構；死時間窗的發射器零逐粒成本；**所有輸出逐位元不變**——這一輪只改快慢，不改結果
- **明確不做**：不做跨幀緩衝重用（純介面下違反引用透明，屬 #7）；不改變任何對外契約；不動粒子上限值

## 功能規劃

只列**尚未交付**的工作。「依賴」欄指向本表的項次；原本依賴已交付項目的，因為沒有東西擋著它，一律記為 `-`。「doc」欄在建立 feature 設計文檔後回填 F 編號。

### 階段三：候選（未動工，逐條有記帳來源）

| # | feature | 一句話說明 | 模組 | 依賴 | doc |
|---|---------|-----------|------|------|-----|
| 1 | drop-intermediate-v3 | 去掉取樣器每粒子的中介向量配置——目前已知的下一個瓶頸 | M2 | - | - |
| 2 | budget-cap-raise | 依既定量測規則重新量測並提高粒子上限，同步對外查詢值 | M2, M1 | #1 | - |
| 3 | velocity-aware-fields | 阻尼／磁力等速度相依的場——需讓場看到速度，等於改熱路徑契約 | M3 | - | - |
| 4 | caster-relative-fields | 施法者相對座標系的力場 | M3 | - | - |
| 5 | leased-buffer-api | 租借式緩衝的**新契約**——純介面下的跨幀重用做不到，要做得換契約 | M1 | - | - |

小結：待交付 **5 個 features、1 個階段**；另有 **2 個已交付**，記於上方「已交付（歷史）」。

## Feature 契約卡

### drop-intermediate-v3

- **階段**：階段三（候選）
- **負責模組**：M2 解析層
- **實作的 Level 2 介面**：無新增，優化 C1.1 的內部路徑
- **資料流管線段落**：業務處理段的取樣路徑
- **驗收標準**：取樣的每粒子配置量顯著下降並有前後對照數字；輸出逐位元不變；序列與平行兩條路徑同時受益
- **明確不做**：不改變任何契約或緩衝表徵；不順手抬粒子上限（屬 #4）

### budget-cap-raise

- **階段**：階段三（候選）
- **負責模組**：M2 解析層、M1 SoA 緩衝
- **實作的 Level 2 介面**：magic-semantics 的預算護欄值改變；本子系統負責提供量測依據
- **資料流管線段落**：不動管線，只改一個常數與其量測
- **驗收標準**：依既定規則（單幀純運算時間不超過某上限的最大二的冪）重新量測並得出新值；對外的查詢值同步；既有法術輸出逐位元不變；C ABI 的凍結常數**零觸碰**（宿主改用查詢）
- **明確不做**：不在沒有量測的情況下改值；不動 C ABI 的凍結常數

### velocity-aware-fields

- **階段**：階段三（候選）
- **負責模組**：M3 力場層
- **實作的 Level 2 介面**：**修改 C3.1**——場加速度改為同時吃位置與速度。這是本子系統唯一一項會動到既有契約的候選
- **資料流管線段落**：業務處理段的「每一步」分支
- **驗收標準**：阻尼與磁力類的場能被表達；現有三種只吃位置的場輸出逐位元不變；每槽位的讀取成本不因新增參數而顯著上升
- **明確不做**：不做粒子對粒子（速度來自該粒子自己的積分狀態，不是鄰居的）；不改變組合點語意

### caster-relative-fields

- **階段**：階段三（候選）
- **負責模組**：M3 力場層
- **實作的 Level 2 介面**：擴充 C3.2 每步輸入，讓場能在施法者座標系中定義
- **資料流管線段落**：業務處理段的「每一步」分支
- **驗收標準**：場可以跟著施法者的位置與面向走，而不是固定在世界座標；以世界座標定義的既有場輸出逐位元不變
- **明確不做**：不讓場隨時間改變參數（屬 magic-semantics 的第四種時間掛載點）

### leased-buffer-api

- **階段**：階段三（候選）
- **負責模組**：M1 SoA 緩衝
- **實作的 Level 2 介面**：**新增**一組租借式的緩衝取得與歸還契約，與 C1.1 的純值契約並存
- **資料流管線段落**：輸出段——緩衝的所有權
- **驗收標準**：宿主能以租借方式重用同一塊記憶體並顯著降低配置；租借契約明文規定緩衝的有效期，且**既有的純值契約完全不變**——兩條路徑並存，舊宿主零改動
- **明確不做**：不把純值契約改成租借式（那會違反引用透明並偷改上一幀）；不預設啟用

