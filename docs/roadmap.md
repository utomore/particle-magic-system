# 路線圖與完整度盤點

> 版本：1.8（2026-08-15，第三波 0015 驗收後修訂：§1 快照、§2 維度、§3.4 三列結清、§5 補第三波交付；1.7 為合併 spec 0016 符文陣設計定案（原分支版號 1.5：§3.4 新增陣形列、§4.7 加候選 F2、§4.8 依賴鏈延長、§5 補第四波）與已併入 main 的 1.6（第二波 0012／0014 驗收＋0015 動工門檻解除）；本次合併純屬兩條平行分支各自獨立編號後的消歧義，不含新決策。1.6 為第二波 0012／0014 驗收後與 0015 設計回合的合併版；1.3 為第一波 0010／0011／0013 驗收後的修訂，1.1 為 spec 0010–0014 設計定案後的修訂，1.0 為 spec 0001–0009 全數交付後的第一次全面盤點）
> 狀態：現況快照＋候選排序。每次 func-spec 驗收後更新本文。
> 相關文件：[architecture.md](architecture.md)（系統長什麼樣）、[SKILL.md](../SKILL.md)（怎麼工作）、[integration.md](integration.md)（宿主怎麼接）

architecture 回答「系統長什麼樣、為什麼」，func-spec 回答「這一輪具體怎麼蓋」，本文回答第三個問題：**還差什麼、下一輪該蓋哪一個**。

本文不做新決策。內容全部由既有文件盤點而來——各 spec 的 §9 非目標、§10 驗收紀錄、architecture §7/§8。真正的取捨仍然寫進 ADR，本文只負責讓「欠了什麼」在一個地方看得見。

---

## 1. 現況快照（2026-08-15）

- **已交付**：func-spec **0001–0017 全數狀態「已完成」**；ADR 0001–0015。第一波（0010 效能 ‖ 0011 宿主整合面 ‖ 0013 視覺）三份由三個 session 平行實作、同時驗收；第二波的 **0012（多陣合成與場景層）** 與 **0014（作者工具）** 亦平行實作、同時驗收；第三波 **0015（視覺表現力・核心半場）** 、第四波 **0016（符文陣）** 與第五波 **0017（陣的駐留）** 各自單獨實作驗收。本輪設計的五個候選 A／B／C／D／E 至此全數落地，候選 F 亦落地。
- **驗證**：`cabal build all` 綠（含 demo exe、`magic-validate` exe、bench、foreign-library）；`cabal test` **1156 examples, 0 failures**（0017 交付後實測；0016 交付時 1123；0015 交付時 1045；0012＋0014 合併後 1018；0012 單獨交付時 969、0014 單獨交付時 952，第一波三份合併時 903，0009 交付時 676）。0007／0008／0009 曾以同一模式三方平行；0010／0011／0013 再次驗證零檔案交集切分成立，0012 對 0014 的零交集亦事後成立（見 0012 §9.5、0014 §9.5）——整合時只有 `docs/roadmap.md`／`SKILL.md`／`.cabal` 的索引列需要人工併行。
- **0012 交付的兩件事**（詳見其 §9）：(a) **粒子上限 4096 → 16384**，依「單幀純 CPU ≤ 2 ms」的規則由實測選定（16384 → 1.45 ms、32768 → 2.87 ms），且 **C header 一字未改**——0011 鋪的查詢鏡射律第一次被實際事件檢驗；(b) **多陣合成落地**（`CompiledSpell` 成為 `Monoid`、`castSpells`）＋**場景層**（`Magic.Scene`，全域配額先到先得），Init.md 對照表最後一列從「未落地」變成測試守護的事實。
- **0014 交付的作者面**（詳見其 §9）：`magic-validate`（boundary 第三個消費者，開不了視窗也畫不了東西）、`docs/spell-schema.md`（作者手冊，鍵名由測試機械守護）、demo 執行中的 spell 清單熱掃描。其手動 smoke 抓到一個純測試看不見的 Windows 路徑分隔符 bug——記在 §3.5。
- **0010 交付的效能位置**（詳見其 §9.2）：4096 粒每幀純 CPU **0.73 ms → 0.27 ms**；取樣常數因子 **161 → 65 ns/粒**；`depthOrder` @4096 快 **10×**；合成 100 000 粒取樣 **6.5 ms**（60 fps 預算的 39%）。
- **0015 交付的展現詞彙**（詳見其 §9）：`BillboardShape` 遷入核心並擴為 4 個無參數建構子（square／soft-dot／ring／spark，宣告序＝C 線碼）、外圈 `StyleRune`＋`"style"` JSON tag、`observeSpell` 依 `(blend, shape)` 相鄰 run-length 分批（零拷貝切片、分割律逐位元）、`PM_SHAPE_*` +3（stride 零觸碰）、demo 程序生成貼圖差異化（零外部資產、不自訂 shader）。既有 10 個範例陣 golden 零重錄；新範例 `soft-bloom.json`。同輪 ADR-0013。
- **0016 交付的符文陣**（詳見其 §9）：新核心模組 `Magic.Sigil`——`hashCircle`（`Circle` 全結構摘要，浮點取位元，**交付即凍結**並入 architecture §11 硬點表）、`sigilPlan`（結構定骨架／摘要定花紋）、六種筆畫的閉式取樣（弧環／星形多邊形／輻條／刻度／玫瑰線／位元遮罩符文帶）、`SpawnOnStroke` 一個建構子接線。**陣不再是霧而是被畫出來的線**——「索引序＝繪製序」律零新機制，直接來自 0002 凍結的 `firstBirth`。`Magic.Codec` 零觸碰、schema 不升版、不加任何符文；力場刻意不進摘要以保住 ADR-0010 D9。逐位元豁免只到 Drawing／Converging（實測 `bare-sigil` 幀 0–89、`grand-sigil` 幀 0–106，之後逐位元相同），代價是這兩個 golden 重錄。新範例 `lattice-seal.json`。同輪 ADR-0014。
- **0017 交付的陣駐留**（詳見其 §9）：陣形發射器的生存窗由 `castStart` 延長至 `ppEnd`、取消陣形收束曲線——**法術從仍然存在的陣中射出**，而不是把陣燒掉。取樣器零變更、預算零變更（`emCount` 一個沒動）、schema 零觸碰；只改了編譯期的兩個數字與一個 `Maybe`。順手修掉一個沒人發現的舊行為：`phConverge < formLife` 的陣（`bare-sigil`）在畫完之前就開始消散。ADR-0010 D6 的「只有施放粒子吃力場」從空保證變成有內容的受測律（重力井吸得動法術，吸不動陣）。逐位元邊界收窄為 `t < min(phDraw, castStart − formLife)`，實測與預測完全吻合。同輪 ADR-0015（取代 ADR-0014 D5）。
- **下一輪未設計**：共 17 份 spec 全數已完成，目前沒有設計定案待實作的 spec。最划算的下一項見 §5 記帳——**陣形旋轉／動態陣形**（0006 §9、0016 §8-3，`skPhase` 已在資料裡，加時間項即可），而 0017 讓它更值得做：一個只存在 1.5 秒的陣沒什麼好轉的，一個存在整場的陣有。

| 產物 | 指令 | 對象 |
|---|---|---|
| demo 執行檔 | `cabal run particle-magic` | 開發者自己看：3D 透視／2D 側視／2D 俯視即時切換、JSON 熱重載、HUD |
| Haskell 公開子庫 | `build-depends: particle-magic:magic-boundary` | Haskell 宿主 |
| C ABI 共享庫 | `cabal build particle-magic-ffi` | Unity／Godot／C++／任何能載入 `.dll`／`.so` 的宿主 |

第三種的存在是本專案「庫必須完整、繪圖在庫外」這條原則的**可執行證明**：`examples/c/main.c` 是一個 150 行、不畫任何東西的 C 宿主，它的逐幀輸出與 Haskell 路徑逐行相同。0011 之後，第三種也拿得到 2D 投影與 painter 排序，並附 C# 參考綁定（`bindings/csharp/`）與 Unity 範例（`examples/unity/`）。

---

## 2. 完整度：先問「對什麼而言」

單一個百分比會騙人，因為這個專案同時是三件事，而三件事的完成度差很多。

| 這是在問…… | 完整度 | 一句話依據 |
|---|---|---|
| **A. 架構論證**——「Circle as Data」這個賭注成不成立 | **≈ 100%** | 五步解釋器 fold、JSON schema v1、決定論、換投影不改核心、換宿主語言不改核心——全部從宣稱變成了測試；0012 補上最後一列「多陣疊加」，且它落地的形式（`CompiledSpell` 是 `Monoid`、合成的正確性是結合律與疊加律）本身就是這個賭注的最強證據。**這一維已經結案**——後面欠的都是產品問題（B／C），不是論證問題 |
| **B. 可被外部遊戲實際使用的庫** | **≈ 70%** | 兩種消費模式都已凍結合約並可用；速度已到 10k–100k 檔次且**護欄已抬到 16384**（0012）；多陣合成與全域配額場景層已有（Haskell 面）。仍欠：場景層無 C ABI、無多執行緒保證、無語言包裝層、無版本 tag |
| **C. 產品級特效系統** | **≈ 50%** | 觀察面已補齊（0013）、熱路徑已優化（0010）、作者回饋圈已補（0014）、展現詞彙已開（0015：四種 billboard 形態＋逐 batch 程序生成貼圖）；仍無拖尾／軟粒子／後處理、無視覺化編輯器 |

### 2.1 對 architecture.md 的逐節達成率（1.4 新增）

上面三個百分比問的是「這個專案作為某種東西完成了多少」。另一個問法是「**架構書寫下的東西，蓋了多少**」——這個問法有明確的答案，因為 architecture.md 的每一節都是可逐條核對的。

| architecture 章節 | 達成 | 依據 |
|---|---|---|
| §1 五個核心理念 | **100%** | Circle as Data、純函數核心、由內而外、維度無關全部從宣稱變成測試；「特效即魔法」的**語意**面成立（槽位職責直接決定粒子數學），表現面的欠款記在 §7／§8 而非此處 |
| §2 系統架構圖 | **100%** | 圖上 14 個模組全部存在，3 條關鍵約束都有機械守護（`BoundarySpec` 讀 cabal 檔）。圖外另有 0011 的 `Magic.Columns`、0013 的 `App.Camera`／`App.Render.Order` |
| §3 資料流（編譯期／每幀／生命週期） | **100%** | 三張圖逐節點落地；四階段生命週期由 0006 交付 |
| §4 核心型別（4.1–4.7） | **100%** | 七小節全數落地。§4.4 那句「結構化 `ParticleBudget` 留待效能 spec」已由 0010 結清 |
| §5 系統介面規格 | **100%** | JSON schema v1、`RenderBatch` 串流、兩種消費模式（Haskell public sublibrary＋C ABI）皆為凍結合約 |
| §6 解釋器（五步 fold＋Init.md 對照表） | **≈ 95%** | 五步 fold 全在；對照表 11 列落地 10 列，**唯一未落地列＝「多個效果疊」**（0012 待實作） |
| §7 效能（6 手段＋吞吐目標） | **≈ 80%** | 吞吐目標實測達標（100k 取樣 6.5 ms）；6 手段 4 項 ✅、1 項（緩衝重用）**經評估改判不做**且理由入檔、1 項部分（`-O2` ✅／`-fllvm`＋多執行緒 ✗）；護欄值 4096 未抬（0012 S1） |
| §8 未來問題（6 條風險） | **≈ 70%** | 4 條已結清或已定政策（組合爆炸交遊戲層、熱重載＝重施法、h-raylib 走 ADR-0009、2D 深度排序）；Expr 加速三階梯只到第一階、2D 可讀性只到第一階、§8.4 全域配額待 0012 |
| §10 容易擴充的地方（7 個擴充點） | 機制 **100%**／實例 **POC 級** | 七個擴充點的機制都在且都被用過至少一次；但值域很小——`FaceShape` 4、`Element` 4、`ForceField` 3、`Trajectory` 4、`BillboardShape` 4（0015 交付；「新符文」「新渲染語彙」兩個擴充點由 `StyleRune` 再各用過一次） |
| §11 不容易擴充的地方 | n/a | 這是代價登記簿，不是待辦。0015 的「`BillboardShape` 永遠無參數」裁決正是對第 3 列（SoA 欄位佈局）的遵守 |

**加權合計 ≈ 90%。** 一句話結論：**架構書幾乎已經蓋完了**。剩下的 10% 裡有一半是架構書自己標成「緩解路徑／可選第二階」的東西（bytecode、`-fllvm`、進階可讀性），不是承諾；真正的明文欠款只有一條——多陣合成，而它已經有 0012 這份設計定案的 spec 在等實作。

所以「還差什麼」的答案要往架構書**之外**找：§2 那張表的 C 欄（產品級特效系統，40%）與作者流程（15%）才是真正的低點，而這兩件事 architecture.md 從來沒有承諾過。這正是 §4.7 三個新候選的來源。

### 分維度明細

| 維度 | 完整度 | 已有 | 缺口 |
|---|---|---|---|
| 架構與純度紀律 | **95%** | 三環依賴、`BoundarySpec` 機械守護、核心零 IO、依賴白名單 | — |
| 魔法語意（表達力） | **80%** | Init.md 參數對照表 **11 列全部落地**（0012 補上多陣合成）；四階段生命週期、力場層、Expr 子系統 | 各 sum type 的建構子數量仍是 POC 級（`FaceShape` 4 種、`ForceField` 3 種、`Element` 少數、`BillboardShape` 1 種）；合成後只有一個 blend mode（ADR-0012 D5 記帳） |
| 對外介面 | **90%** | Haskell public sublibrary＋C ABI header 兩份凍結合約；決定論跨界為可測等價律；投影／painter 排序／上限查詢已上 C ABI（0011），C# 參考綁定受契約測試守護且在 Unity 6000.5.7f1 實測通過；上限提升已實證不動 header（0012） | 多 spell 聚合／場景層**只在 Haskell 面**（`pm_scene_*` 不存在，ADR-0012 D8）；無 release tag／Hackage；GDScript／C++ 包裝層 |
| 效能 | **80%** | 0010：熱路徑端到端 unboxed（count-then-fill、`FieldState` SoA、in-place introsort）、結構化 `ParticleBudget`、發射器時間窗剔除、Expr 常數摺疊、per-emitter 基底提升；10k–100k 已實測（100k 取樣 6.5 ms）。0012：**護欄值 4096 → 16384**，由整幀（取樣＋quad）量測選定 | architecture §7 六項手段中「緩衝重用」在純介面下經評估不做（0010 §9.4-10）、architecture §8.2 的 Expr bytecode 與多執行緒取樣仍未做——它們也是下一次抬高上限的前提（ADR-0012 §後果） |
| 視覺表現力 | **60%** | 兩個投影後端、blend 生效、顏色曲線、painter 排序（2D）；0013：3D alpha batch 深度排序、軌道／dolly 相機、2D 平移／游標定錨縮放／resize 適配、俯視深度色調；0015：四種 billboard 形態（`StyleRune` 玩家可選）、多批次輸出、逐 batch 程序生成貼圖 | 無拖尾／軟粒子／後處理（0015 §8-1/8-2 記帳）；俯視可讀性只解到第一階（0013 §8-4）；**發動前的陣形只有同心環帶且是亂數散佈而非筆畫**（0016 認領） |
| 作者流程（工具） | **55%** | JSON 熱重載、載入錯誤上屏（含行列位置）、9 個範例陣；0014：`magic-validate` 驗證 CLI（exit code＝失敗檔數）、`docs/spell-schema.md` 作者手冊（鍵名由測試守護）、spell 清單執行中熱掃描 | 無視覺化編輯器／即時預覽 UI（0014 §8-1）、無機器可讀 JSON Schema（0014 §8-3）、fsnotify 仍以 2 秒輪詢代替 |
| 工程紀律（測試／文件） | **95%** | Todo↔測試 1:1、逐位元相容律、golden 鎖定、契約守護測試（cabal／header／`.def`／C# 綁定四份文本＋schema 文件鍵名）、13 ADR／**15 spec 全數完成**／**1045 examples** | 無 CI（門檻已因 `magic-validate` 降低）；只有 win64 實測過 |

---

## 3. 未落地盤點（依記帳來源）

每一條都在某份 spec 的 §9 裡有主。這是欠款總表，不是願望清單。

### 3.1 效能（0010 已結清大部分；下表為結清後的餘額）

| 項目 | 記帳於 | 0010 之後 |
|---|---|---|
| `ParticleBuffer` 緩衝重用（`ST` 內部、對外仍純） | 0005 §9、architecture §7 | **改判：純介面下不做**。`observeSpell` 回傳的 buffer 是宿主可長期持有的純值，跨幀重寫會偷改上一幀（違反 ADR-0007）。正解是「每幀恰好六次 exact-size 配置、零中介」，0010 已落地；真正的 mutable 重用需要不同的 API 合約，且 §9.2 的量測顯示配置不是瓶頸（0010 §9.4-10） |
| `fromParticles` 的 list 中介消除 | 0005 §9 | ✅ 0010 S2（`buildBuffer` count-then-fill；`fromParticles` 降為相容薄包裝） |
| 結構化 `ParticleBudget` 取代裸 `spellBudget`／`budgetCap` | 0004 §9、0006 §9、architecture §4.4 | ✅ 0010 S7（`ParticleBudget`＋`spellBudgetPlan`；`spellBudget` 保留為總和） |
| 發射器層級剔除（Expr 靜態範圍分析） | 0007 §9、architecture §7 | ✅ 0010 S3（時間窗剔除，`aliveRanges`）＋S7（`emitterBounds` 的區間算術 AABB）。**視錐剔除本體仍是宿主責任**，核心只交包絡（0010 §8-3） |
| `FieldState` SoA 化＋帶場 bench | 0007 §9、§4.7 明文不凍結 | ✅ 0010 S4＋S8（表徵**仍不凍結**） |
| `depthOrder` 的高效排序（現為 `sortOn` 產生 boxed list） | 0008 §9-6 | ✅ 0010 S5（in-place introsort，@4096 快 10×） |
| 3D 路徑的深度排序 | 0005 §9、0008 §9-1 | ✅ **0013 S1 交付**（`App.Render.Order.viewOrder`，`app/*` staging 層；該處以 `sortOn` 起步，若日後成為熱點可套 0010 S5 的 in-place introsort，記在 0013 §8-6） |
| Expr 求值加速（SPECIALIZE → 常數摺疊 → bytecode） | architecture §8.2 | 第一階 ✅（0010 S6：`foldConstants`＋`INLINE`/`INLINABLE`）；**bytecode／共同子式消去未做**（0010 §8-4） |
| 10k–100k 吞吐 | architecture §7 目標 | ✅ 已實測（0010 §9.2）：10k = 0.65 ms、50k = 3.1 ms、100k = 6.5 ms（60 fps 預算的 3.9%／19%／39%） |
| 粒子上限值的提升 | 0010 §9.2、0011 §8-1 | ✅ **0012 S1**：4096 → **16384**，規則＝「單幀純 CPU（取樣＋quad）≤ 2 ms 的最大 2 的冪」，實測 1.45 ms |
| 多執行緒取樣（`-threaded` 平行 sample） | 0010 §8-6 | 未做——先把單執行緒常數因子吃完，已吃到 65 ns/粒。連同 Expr bytecode，是**下一次抬高上限的前提**（ADR-0012 §後果） |

**這個結構性問題已經結清**：`4096` 曾有三份拷貝——核心的 `Magic.Compile.budgetCap`、demo 的 `App.Render.Raylib3D.gpuCapacity`、C 合約的 `PM_MAX_PARTICLES`，且 `test/FFIContractSpec.hs` 把三者釘成相等（§4.6 修正 1）。三步拆解如設計走完：0010 不改值、只把它第一次經公開面匯出（`maxSpellParticles`）；0011 把釘選改寫為「header 永釘 ＋ 查詢鏡射律」；0012 S1 改值。結果：核心與 `pmMaxParticles` 兩處同步（一行），header **零觸碰**，而 `gpuCapacity` 自此**不再是拷貝**——它降級為 GPU 上傳粒度（`Word16` 索引上限使然），超量 batch 改為分塊繪製（`App.Render.Chunk`）。三份拷貝降為兩份，第三份是查詢。

### 3.2 語意

| 項目 | 記帳於 |
|---|---|
| ~~多陣合成（`CompiledSpell` 的 `Semigroup`、`PhasePlan` 合併律、預算超額的 `Either`）~~ | 0006 §9、architecture §6 → ✅ **0012 S2/S3**（逐界標 max、`compileMany`、`castSpells`；ADR-0012 D1–D3） |
| ~~全域粒子配額與多 `ActiveSpell` 緩衝管理~~ | architecture §8.4 → ✅ **0012 S4**（`Magic.Scene`，先到先得；進階配額策略明文否決，ADR-0012 D6） |
| 合成後的 per-component blend（現為第一張陣的） | ADR-0012 D5；修法＝`observeSpell` 依 blend 分批，屬對外合約演進，留給視覺輪 |
| 隔離合成（per-emitter 場路由，各陣的場只管各陣） | ADR-0012 D4：v1 裁決為完全融合，無需求前不做 |
| `Anchor` 的玩家面 JSON 控制 | 0006 §9 |
| 陣形旋轉／動態陣形動畫 | 0006 §9 |
| Expr 驅動的時變場參數（第四種時間掛載點） | 0007 §9 |
| 施法者相對座標系的場 | 0007 §9 |
| 場作用於 Drawing／Converging 相位 | ADR-0010 D6（使用者裁決不做） |
| 可調 Dissipating／全域消散拖尾 | 0006 §9 |

### 3.3 介面／宿主

| 項目 | 記帳於 | 備註 |
|---|---|---|
| ~~`pm_project`／`pm_depth_order` 上 C ABI~~ | 0009 §9-1 | ✅ **0011 交付** |
| 多 spell 聚合／全域配額 FFI API（`pm_scene_*`） | 0009 §9-7、0011 §8-2、**0012 §8-1／ADR-0012 D8** | 前置（多陣合成＋場景層）已於 0012 交付；**C ABI 面刻意延後**——場景層 API 應先在 Haskell 面用過一輪再凍結成 C 合約（header 只加不改，C 面的錯誤貴得多）。非 Haskell 宿主目前自行持有多個 `PmSpell*` 記帳 |
| GDScript／C++ 包裝層 | 0009 §9-6、0011 §8-3 | C# 已有參考實作（0011 S4）；其餘等真實宿主需求 |
| ~~C# 綁定在 Unity Editor 的實測~~ | 0011 §9.3 | ✅ Unity 6000.5.7f1 batchmode 實測 27 PASS／0 FAIL；`examples/unity/PmSmoke.cs` 可一鍵複驗 |
| Unity Editor 的「二次 Play」與視覺／GC 觀察 | 0011 §9.3 | 批次模式驗不到（每次新 process）；README §8 為人眼 checklist |
| 多執行緒安全／內部鎖 | 0009 §9-2、0011 §8-4 | 等真實宿主需求 |
| 熱重載 FFI API | 0009 §9-5、0011 §8-5 | 政策已定：重載＝重施法，宿主自行 `pm_cast` |
| ~~座標系手性未見於任何文件~~ | §4.4 | ✅ **0011 寫進 header**（哨兵 `right-handed`，契約測試守護） |
| ~~`pbColor` 的位元組序未寫進 C header~~ | §4.4 | ✅ **0011 寫進 header**（哨兵 `0xRRGGBBAA`，附拆包示例） |
| ~~上限提升（4096 → 量測值）~~ | 0011 §8-1 → 0012 S1 | ✅ **0012 交付：16384**。管道如設計運作——header 零字元變更、既有宿主零重編譯、10 個範例陣 240 幀 golden 逐位元不變 |

### 3.4 視覺

| 項目 | 記帳於 |
|---|---|
| ~~俯視深度重疊的可讀性解~~ 第一解（深度色調）**已交付 0013**；進階解（壓平比例、輪廓強調）仍欠 | 0008 §9-8、architecture §8.6、0013 §8-4 → 候選 G |
| ~~`BillboardShape` 新建構子~~ | 0005 §9、0013 §8-1 → ✅ **0015 交付**：4 個無參數建構子＋`StyleRune`；帶參數的拉伸 billboard 因 `PM_BATCH_INFO_STRIDE` 凍結而明文不做（0015 §8-1／ADR-0013 D1） |
| ~~`rbShape` 差異化渲染~~ | 0008 §9-5、0013 §8-1 → ✅ **0015 交付**：S1 分批＋S4 逐 batch 程序生成貼圖（預設 shader 的 diffuse map，零外部資產） |
| ~~`observeSpell` 只回傳單一 batch~~ | **1.4 盤點新發現** → ✅ **0015 S1 交付**：依 `(blend, shape)` 相鄰 run-length 分批。複數 blend 的批次仍要等多元素魔法陣（0015 §8-5——blend 仍只由 `Element` 決定） |
| ~~2D 相機平移／縮放、視窗 resize 適配~~ **已交付 0013** | 0008 §9-3 |
| ~~3D 相機操控（軌道／縮放）~~ **已交付 0013** | 0005 §9 |
| 拖尾／軟粒子／後處理 | 0013 §8-3（產品級視覺，維度 C） |
| 相機動畫／過場、慣性阻尼 | 0013 §8-5 |
| ~~**陣形幾何只有同心環帶，且取樣是面積均勻亂數散佈**（`sampleShape` ⇒ 陣是霧不是線）~~ | **1.5 盤點新發現** → ✅ **0016 交付**（`Magic.Sigil`：`hashCircle` 導出、六種筆畫、索引序＝繪製序）。註：`sampleShape` 的面積均勻取樣本身**未改**——它仍是 `SpawnOnShape`（玩家畫的形狀、施放主體）的正確語意；改的是陣形改走 `SpawnOnStroke` |
| 陣形旋轉／動態陣形動畫 | 0006 §9、0016 §8-3、0017 §8-2（`skPhase` 已在資料裡，加時間項即可——下一輪最划算的一項，且 0017 讓陣活滿整場後更值得做） |
| ~~陣在施放瞬間消失（陣被消耗掉而不是法術從陣裡射出）~~ | **0017 實作時的使用者需求** → ✅ **0017 交付**：生存窗延至 `ppEnd`、取消收束 |
| 陣的獨立時間軸（法術結束後陣再留一會／陣比法術先收） | 0017 §8-3（需新 JSON 鍵如 `phases.linger`，屬 schema 擴充） |
| 靜止不重畫的陣（目前以 `formLife` 為週期循環重畫＝「呼吸」） | 0017 §8-1（需「畫完即凍結」的新排程語意，不是調參數） |
| 符文文字的**語意**（字符集、字義影響魔法） | 0016 §8-1（其 `GlyphBand` 只產生線段，不表義） |
| 3D 立體陣（多層平面沿法線堆疊） | 0016 §8-4 |

### 3.5 系統／流程

| 項目 | 記帳於 |
|---|---|
| ~~spell 清單熱掃描（啟動時定格）~~ | **0014 S3 已結清**：`ScanDir` op＋2 秒節流輪詢，選擇依路徑保持、刪除落到相鄰項 |
| ~~作者面 schema 文件、檔案驗證工具~~ | **0014 S1／S2 已結清**：`docs/spell-schema.md`＋`magic-validate` |
| fsnotify（仍以輪詢代替） | 0005 §9、ADR-0005 既定延後；0014 §8-2 明文再次不做（10 檔規模輪詢零成本） |
| 視覺化編輯器／即時預覽 UI | 0014 §8-1：等有非工程作者為止 |
| JSON Schema（draft-07 等機器格式）輸出 | 0014 §8-3：等外部工具鏈需求 |
| CI（目前全靠本機 `cabal test`） | 未記帳。**0014 後門檻降低**：`magic-validate` 的 exit code＝失敗檔數，資產檢查已可一行進 CI |
| release tag／版本發布流程 | 未記帳 |
| 手動 smoke 的必要性 | 0014 §9.2：腳本化的 headless 測試依定義看不到 IO 解譯器產生的字串本身（Windows 路徑分隔符使「重掃 ≡ 啟動清單」的等式失效，程式照跑、只有 HUD 看得出來）。凡是「IO 端產生、純端比較相等」的地方，都要有一條打真檔案系統的守護測試 |

---

## 4. 候選 func-spec 與排序

### 4.1 候選一覽

| 候選 | 主題 | 主要檔案 | 依賴 |
|---|---|---|---|
| **A** | **效能與粒子預算治理**（10k–100k） | `Buffer`／`Analytic`／`Field`／`Compile`／`Interface`／`bench` | 無（動工前提「0005 量測基線」已備） |
| **B** | **宿主整合面**（投影上 C ABI＋參考綁定） | `src/ffi`／`include`／`examples`／新 `bindings/` | 無（0008 完成後解鎖） |
| **C** | **多陣合成與場景層** | `Compile`／`Interface`＋新聚合型別 | 建議在 A 之後（全域配額要靠 A 的 `ParticleBudget` 表達） |
| **D** | **視覺表現力** | `app/*`（＋若動 `BillboardShape` 則含 `Compile`） | 無（限縮在 `app/*` 時） |
| **E** | **作者工具**（驗證 CLI、schema 說明、清單熱掃） | 新 exe／`Codec` | 無 |

### 4.2 為什麼建議 **A（效能）作為 0010 主線**

1. **它是被指名最多次的一筆**：0002、0004、0005、0006、0007、0008 六份 spec 的 §9 都把東西記到「效能 spec」名下。這個名字已經被欠了六輪。
2. **它決定 B 這件事的成敗**：架構論證（完整度 A ≈ 85%）已經很高，可用的庫（B ≈ 55%）低在哪裡？一半低在 4096。宿主接得上、但只能放 4096 顆粒子的庫，離「可用」還有距離。
3. **動工門檻已經到位**：0005 交付的 bench 基線就是為了這一輪存在的，這是唯一一份「前置條件早就備好、只是還沒動工」的候選。
4. **它必須先於任何綁定層被凍結**：每個宿主綁定都會把 `PM_MAX_PARTICLES` 這個數字烤進去。先做綁定再改上限＝破壞既有宿主。

**A 會撞到的一個 ABI 問題，以及乾淨的解**：提高上限意味著 `PM_MAX_PARTICLES` 這個常數的值要變，而 header 是「只加不改」的凍結合約——改常數值是破壞性變更。乾淨解是**新增一個執行期查詢** `int pm_max_particles(void)`，把 header 常數降級為「ABI 第 1 代編譯時的值」，宿主改用查詢結果配置緩衝。這是純增補，完全符合 add-only 規則。

值得注意的是：**這個新匯出屬於 B 的檔案範圍，而它的正確性不依賴 A 做完**——現在加也對（回傳 4096），A 改了核心上限之後它自動回傳新值，header 一個字都不用再動。這讓 A 與 B 可以真正平行：B 先把查詢管道鋪好，A 只管改核心的數字。

### 4.3 為什麼 B 可以與 A **平行**（SKILL.md 規則 4）

| | A 的檔案 | B 的檔案 |
|---|---|---|
| 核心 | `src/core/Magic/{Compile,Particle/*}.hs` | 零觸碰 |
| 邊界 | `src/boundary/Magic/Interface.hs` | 零觸碰 |
| FFI | 零觸碰 | `src/ffi/Magic/FFI.hs`、`include/particle_magic.h` |
| 新目錄 | — | `bindings/csharp/`、`examples/unity/` |
| 殼層 | `app/App/Render/Raylib3D.hs`（`gpuCapacity`） | 零觸碰 |
| 共用 | cabal（`magic-core`／test 行）、SKILL.md（索引列） | cabal（foreign-library／test 行）、SKILL.md（索引列） |

交集為空，只有 cabal 與 SKILL.md 的**不同行**——與 0007／0008／0009 那一輪同款的聯集合併。**A 與 B 可由兩個 session 同時認領。**

B 的具體交付面：`pm_project`／`pm_depth_order`／`pm_max_particles` 三個純增補匯出（`FFIContractSpec` 自動守護 header↔export 一致）、C# 參考綁定與 Unity 最小範例（比照 `examples/c/main.c` 的「手動 smoke」定位）、以及把 [integration.md](integration.md) 裡的程式碼片段變成真的會編譯的檔案。

### 4.4 B 應該順手補上的兩個**文件缺口**（本次盤點發現）

兩者都是宿主一定會踩、而目前只存在於程式碼裡的事實：

1. **座標系手性**：核心用的是 OpenGL 式右手系——X 右、Y 上、**+Z 朝觀者**。證據是 `orthographic SideXY` 的 depth = −z（z 越大越近）與 raylib 3D 後端。Unity 是左手系（+Z 進畫面），所以 Unity 宿主必須翻 Z；不翻的話，`vortex` 場的旋轉方向會反過來（`cross` 用的是標準公式，手性是真的被固定住的）。目前**沒有任何文件寫過這件事**。
2. **`pbColor` 的位元組序**：實際是 `0xRRGGBBAA`（R 在高位、A 在低位，由 `Analytic.rampColor` 與 `Compile.clearAlpha = c .&. 0xFFFFFF00` 確證）。C header 的 `uint32_t* color` 一欄沒有說，宿主只能猜或反組譯。

第 2 點可以只在 header 加註解（純註解、不動任何宣告，仍在 add-only 規則內）。

### 4.5 C／D／E 的位置

- **C（多陣合成）** 是完整度維度 A 剩下那 15% 的全部——Init.md 對照表最後一列。放在 A 之後的理由是檔案衝突（兩者都改 `Compile`／`Interface`）與語意依賴（全域配額要用結構化預算表達）。真要提前，代價是 A 與 C 必須排序而非平行。
- **D（視覺）** 若限縮在 `app/*` 就與所有其他候選零交集，可以當第三條平行線；但一旦要加 `BillboardShape` 建構子就會撞到 `Compile`。建議先做純 `app/*` 的那半（3D 深度排序、俯視可讀性、相機操控）。
- **E（作者工具）** 是 POC 階段最容易延後、也最沒有損失的一項——現在的作者就是寫程式的人，熱重載＋錯誤上屏已經夠用。等到有非工程的作者時再做。

### 4.6 設計輪的兩個修正（2026-08-14，spec 0010–0014 設計時發現）

寫 spec 時逐檔盤點，§4.2／§4.3 的論證有兩處要修：

1. **4096 不只三份拷貝，還被測試釘成三方相等**：`test/FFIContractSpec.hs` 斷言 `PM_MAX_PARTICLES == pmMaxParticles == budgetCap == 4096`，而 `pmMaxParticles` 住在 `src/ffi/Magic/FFI.hs`——A 只要改 `budgetCap` 的**值**就必須同時改 B 的兩個主檔案，§4.3 的零交集即破。修正：**A（0010）本輪不改值**（結構化預算、SoA、量測照做；大規模吞吐以直接建構的 `CompiledSpell` 量），B（0011）交付 `pm_max_particles` 時把釘選改寫為「header 常數永釘 4096（第 1 代值）＋查詢鏡射律 `pm_max_particles ≡ budgetCap`」，**上限值的實際提升落在 C（0012）的 S1**——新值依 0010 的量測選定，届時 header 零觸碰。§4.2 的「乾淨解」仍然成立，只是分工從「B 鋪管道、A 改數字」修正為「B 鋪管道並鬆綁、C 改數字」。
2. **A 其實不必碰 `app/*`**（不改 cap 值 ⇒ `gpuCapacity` 不動），§4.3 表中 A 的殼層列作廢。於是 **D（0013，限縮 `app/*`）可以提前到第一波**，與 A、B 三方平行——複製 0007／0008／0009 三方零交集的成功結構。

另外兩處小修：`pm_depth_order` 需要新的 boundary 建構面（`Magic.Columns`，新檔案，見 0011 §0.3——boundary 凍結匯出刻意不開 `ParticleBuffer` 建構子，FFI 無法直接組 buffer）；E 的熱掃描與 D 撞 `app/Main.hs`／`Loop`／`Effects`，故 E（0014）動工門檻含 0013 驗收。

### 4.7 第三波候選（1.4 新增：A–E 五個候選全部有 spec 之後的下一批）

候選 A–E 對應 spec 0010–0014，至此全部有 spec。§2.1 的盤點指出下一批該往哪走——不是架構書裡剩下的零頭，而是架構書沒承諾、但「可出貨」需要的東西：

| 候選 | 主題 | 主要檔案 | 依賴 | 狀態 |
|---|---|---|---|---|
| **F** | **視覺表現力・核心半場**：展現詞彙（`BillboardShape` 擴充＋`StyleRune`）、`observeSpell` 多批次、逐 batch 差異化貼圖 | `Rune`／`Compile`／`Codec`／`Interface`／`FFI`／header／`Render/{Quads,Raylib3D}` | **0012**（五檔交集） | **= spec 0015，設計定案** |
| **F2** | **符文陣**：陣形幾何由 `hashCircle` 從魔法陣資料導出——`Magic.Sigil` 新核心模組、六種筆畫、索引序＝繪製序 | 新 `Magic/Sigil.hs`、`Compile`／`Analytic`（各一處接線） | **0015**（同碰 `Compile.hs`） | **= spec 0016，設計定案** |
| **G** | **魔法語彙擴張**：`FaceShape`／`Element`／`Trajectory`／`ForceField` 由 POC 級擴到遊戲級；順帶收俯視進階可讀性 | `Rune`／`Compile`／`Codec` | 0015（同碰 `Rune`／`Compile`／`Codec`） | 未設計 |
| **H** | **工程化**：CI（目前全靠本機 `cabal test`）、release tag／版本發布流程、非 win64 平台實測 | `.github/workflows/`（新）、README、cabal 版本行 | **無**——與所有進行中的 spec 檔案零交集 | 未設計 |

四者的性格差很多：**F 是命題級的欠款**（「特效即魔法」而目前所有魔法都是同一種方塊）、**F2 是「Circle as Data」第一次變成看得見的東西**（資料不只決定粒子怎麼飛，還決定陣長什麼樣）、**G 是廉價的值域擴充**（architecture §10 已經保證便宜）、**H 最便宜且隨時可插隊**（唯一一條現在就能與任何人平行的線）。

**候選 F 為什麼排在 G 前面**：G 加的是「同一種表現手法的更多參數值」，F 加的是「表現手法這個維度本身」。維度值域為 1 的時候，先擴維度再擴值域。

**F2 為什麼排在 F 之後**：兩者同碰 `Compile.hs`，只能排序。先 F 的理由是符文陣的筆畫用軟光點畫遠比用方塊好看，而且 F 的多批次輸出讓陣形與主效果可以各用不同形狀——F2 的視覺成果會因為 F 先到而變好，反之則否。

### 4.8 依賴鏈（1.4 新增，1.5 延長）

```
0010 ─┬─> 0012 ──> 0015 ──> 0016 ──> (G)
0011 ─┘     ‖         ‖        ‖
0013 ─────> 0014 ─────┴────────┘
                     H（任何時候，零交集）
```

`‖` ＝ 可平行。0014 與 0012／0015／0016 三者皆平行（它只碰 `tools/` 與 `app/` 的迴圈半場）；0015 必須排在 0012 之後（`Compile`／`Interface`／`FFI`／`FFIContractSpec`／`Raylib3D` 五檔交集）、0016 必須排在 0015 之後（`Compile.hs` 一檔交集）。核心那條鏈是串行的，因為 `Compile.hs` 是解釋器的所在地——五步 fold 的每一步都住在裡面，而這三輪各自要動不同的步驟。

---

## 5. 建議的下一步（1.6 修訂：三波圖，第二波已驗收、第三波動工門檻已解除）

spec 0010–0014 對應候選 A／B／C／D／E，依 §4.6 的修正排為三波：

- **第一波：三份全部驗收完畢（2026-08-15）** — ~~0010 = A（效能）~~ ✅ ‖ ~~0011 = B（宿主整合面）~~ ✅ ‖ ~~0013 = D（視覺，`app/*` 半場）~~ ✅。三方檔案零交集，各 spec §0.2 的逐檔證明事後全部成立，整合時只有 `SKILL.md`／`docs/*`／`.cabal` 的索引列需要人工併行。
- **第二波：兩份全部驗收完畢（2026-08-15）** — ~~**0012 = C（多陣合成與場景層＋上限正式提升）**~~ ✅ ‖ ~~**0014 = E（作者工具）**~~ ✅。兩份由兩個 session 平行實作、同時驗收，各自的檔案盤點事後成立（0012 §9.5-5 另更新了五個測試檔中寫死的 4096，同樣不與 0014 相交），整合時只有 `docs/roadmap.md` 與 `.cabal` 的索引列需要人工併行。
- **第三波：驗收完畢（2026-08-15）** — ~~**0015 = F（視覺表現力・核心半場）**~~ ✅。單一 session 實作；S0 spike 證實 h-raylib 貼圖路徑可行（texcoord 填值＋`loadTextureFromImage`＋diffuse 槽 poke），未啟用退場方案。與 0012 交集的五檔在 0012 之後串行，如設計。
- **第四波**：**0016 = F2（符文陣）**，設計定案、**動工門檻＝0015 驗收**（`Compile.hs` 交集）——**已隨第三波達成而解除**，可以立刻動工；**與 0014（已完成）平行**。候選 G／H 未設計（§4.7）。

理由一句話：A 把速度做出來（取樣快 3.4×、100k 粒 6.5 ms）、B 把管道鋪出去、D 把畫面看清楚——三件事互不碰檔案；C 用 A 的量測與 B 的鬆綁把 4096 抬到 16384 並補上最後一列語意、E 把作者回饋圈補完；下一步是 F，讓畫面上的東西**值得看**——0013 解決的是「看得清楚」，0015 解決的是「看的是什麼」，而它現在可以立刻動工，不必再等；F2（0016）則讓發動前的那個陣變成**這個魔法自己的**陣，但要等 F（0015）先落地。

---

## 6. POC 與產品之間的分水嶺

以下三件事沒做完之前，這套系統適合「證明想法」，不適合「出貨」：

1. ~~**效能**（候選 A）~~——**已跨過**：熱路徑於 0010 交付（100k 粒取樣 6.5 ms），護欄值於 0012 抬到 **16384**（單幀 1.45 ms）。剩下的是「再往上」的問題（Expr bytecode、多執行緒取樣），不是「能不能用」的問題。
2. ~~**多陣合成與全域配額**（候選 C）~~——**已跨過**：0012 交付合成（`castSpells`）與場景層（`Magic.Scene`）。剩下的是它還沒上 C ABI（§3.3），以及合成後只有一個 blend mode（ADR-0012 D5）。
3. ~~**視覺表現力**（候選 D→F）~~——**已跨過**：觀察面於 0013 交付（3D alpha 深度排序、軌道相機、2D 平移縮放、俯視深度色調），表現面於 0015 交付（`BillboardShape` 1 → 4、外圈 `StyleRune`、`observeSpell` 多批次、逐 batch 程序生成貼圖）。粒子形態這個語意維度的值域從 1 變成 4，「特效即魔法」第一次有了玩家可寫的展現詞彙。剩下的是語彙的廣度（候選 G）與陣形筆畫（0016），不是「有沒有」的問題。

反過來說，**架構本身不在這張清單上**。三環依賴、決定論、Circle as Data、維度無關、兩種消費模式——這些都已經是有測試守護的既成事實，後面每一輪都是在這個地基上加東西，而不是回頭改地基。這是目前這個專案最大的一筆資產。
