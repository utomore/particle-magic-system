cabal-version:      3.4
name:               particle-magic
-- Four segments, PVP. The release policy lives in docs/release.md and
-- ADR-0016; the short version is that magic-core and magic-boundary are
-- public sublibraries, so a breaking change to a frozen interface is a
-- major bump, and the git tag is 'v' ++ this string. PM_ABI_VERSION in
-- include/particle_magic.h is deliberately NOT tied to this number: the
-- header is add-only, so the two generations move independently.
-- test/ReleaseMetaSpec.hs keeps this in step with CHANGELOG.md.
version:            0.1.0.0
-- The compilers this has actually been built and tested with, not the
-- ones it might work with. Must equal the GHC version CI installs
-- (.github/workflows/ci.yml), which ReleaseMetaSpec asserts.
tested-with:        GHC ==9.14.1
synopsis:           Particle magic system proof-of-concept
description:
    A pure-core, data-driven particle magic system: magic circles are
    Haskell ADTs (JSON-serializable) compiled into deterministic particle
    emitters. The public libraries (magic-core, magic-boundary) are
    renderer- and engine-agnostic; this repo's executable is an h-raylib
    demo shell. Host games depend on particle-magic:magic-boundary and
    consume Magic.Interface / Magic.Codec only.
license:            MIT
license-file:       LICENSE
maintainer:         utomore@outlook.com
category:           Graphics
build-type:         Simple
extra-doc-files:    README.md
                    CHANGELOG.md
-- The C ABI contract and its example host (func-spec 0009): shipped so a
-- non-Haskell consumer gets the header and a working reference host.
-- The C# reference binding joins them in func-spec 0011: same purpose,
-- next language along (test/BindingContractSpec.hs keeps it in step with
-- the header).
-- examples/haskell/ joins them in enhance-0001, closing the odd gap that
-- the two non-native routes had runnable proof and the native one did
-- not. It is a package of its own (not a stanza below) precisely so it
-- consumes this one from the outside; test/ExampleHostSpec.hs asserts
-- that every file of it is listed here.
extra-source-files: include/particle_magic.h
                    examples/c/main.c
                    examples/c/scene.c
                    bindings/csharp/ParticleMagic.cs
                    examples/haskell/Main.hs
                    examples/haskell/particle-magic-example.cabal
                    examples/haskell/cabal.project
                    examples/haskell/expected-output.txt
                    examples/haskell/README.md
                    test/oop/oop_smoke.c
                    test/oop/build.sh
                    test/oop/build.ps1
                    test/oop/run.sh
                    test/oop/run.ps1
                    test/oop/README.md
                    test/oop/particle-magic-ffi-poison.def
                    test/oop/poison/Magic/FFI/Poison.hs

source-repository head
    type:     git
    location: https://github.com/utomore/particle-magic-system.git

-- The exception firewall (host-runtime C2.1) answers a bug inside this
-- library. No legitimate call produces one -- a call that did would be a
-- defect to fix rather than a fixture to test with -- so the only honest
-- way for the out-of-process smoke to watch the firewall work is to build
-- a SEPARATE library carrying one deliberate trigger (host-runtime F006
-- T9, design.md M8).
--
-- `manual: True` and `default: False` mean solver-driven builds never turn
-- it on: `cabal build all` does not build the poison library at all, which
-- matters because a standalone DLL is expensive to link. The stanza below
-- also gives its product a DIFFERENT FILE NAME, which is a harder guard
-- than the flag: a poison build can never be mistaken for a shipped drop.
flag oop-poison
    description: Build particle-magic-ffi-poison, the test-only library
                 whose one extra symbol triggers the exception firewall
                 from C. Never shipped; see test/oop/README.md.
    default:     False
    manual:      True

-- Architecture boundary is enforced by this package structure
-- (func-spec 0001 §3, guarded by test/BoundarySpec.hs):
--   * magic-core: pure core, build-depends whitelist {base, vector, deepseq}
--   * magic-boundary: pure boundary layer (JSON codec, public interface)
--   * executable: effectful shell; depends on magic-boundary ONLY,
--     so it physically cannot import core internals.

library magic-core
    visibility:       public
    hs-source-dirs:   src/core
    exposed-modules:  Magic.Types
                      Magic.Particle.Buffer
                      Magic.Rune
                      Magic.Circle
                      Magic.Sigil
                      Magic.Compile
                      Magic.Space
                      Magic.Particle.Analytic
                      Magic.Particle.Field
                      Magic.Project
                      Magic.Expr
                      Magic.Expr.Code
    -- 'parallel' joins the core whitelist in func-spec 0022 (ADR-0017):
    -- Control.Parallel.Strategies is a *pure* API -- 'using' only chooses
    -- an evaluation order, never a value -- so the per-emitter parallel
    -- sampler keeps ADR-0007's "no IO, no Eff in the core" intact, which
    -- forkIO/unsafePerformIO could not have. First widening of this list
    -- since func-spec 0001; test/BoundarySpec.hs guards the new one.
    build-depends:    base     ^>=4.22
                    , vector   ^>=0.13
                    , deepseq  ^>=1.5
                    , parallel ^>=3.3
    default-language: GHC2021
    ghc-options:      -Wall -O2

library magic-boundary
    visibility:       public
    hs-source-dirs:   src/boundary
    exposed-modules:  Magic.Codec
                      Magic.Interface
                      Magic.Projection
                      Magic.Step
                      Magic.Expr.Parse
                      Magic.Columns
                      Magic.Scene
    -- 'vector' joins the boundary layer in func-spec 0007: Magic.Interface
    -- feeds the force-field layer per-slot inputs and adds its
    -- displacements onto the sampled SoA buffer. Already inside the
    -- whitelist test/BoundarySpec.hs guards.
    build-depends:    base                 ^>=4.22
                    , particle-magic:magic-core
                    , vector               ^>=0.13
                    , aeson                ^>=2.3
                    , bytestring           ^>=0.12
                    , text                 ^>=2.1
                    , megaparsec           ^>=9.8
                    , parser-combinators   ^>=1.3
    default-language: GHC2021
    ghc-options:      -Wall -O2

executable particle-magic
    main-is:          Main.hs
    hs-source-dirs:   app
    other-modules:    App.Effects
                    , App.HotReload
                    , App.Loop
                    , App.TestInterp
                    , App.Camera
                    , App.Render.Raylib3D
                    , App.Render.Quads
                    , App.Render.Flat
                    , App.Render.Order
                    , App.Render.Chunk
                    , App.Render.Sprite
                    , App.Render.Shader
                    , App.Render.Post
                    , App.Scene
                    , App.Hud
                    , App.Panel
    build-depends:    base       ^>=4.22
                    , particle-magic:magic-boundary
                    , h-raylib   ^>=5.6
                    -- func-spec 0024 S4: the parameter panel edits a
                    -- circle through its canonical JSON, because this
                    -- layer cannot see the ADT -- magic-core is out of
                    -- reach by design and Magic.Interface exports Circle
                    -- opaquely. App.Panel's haddock argues that this is
                    -- the better of the two designs, not a workaround.
                    , aeson      ^>=2.3
                    , effectful  ^>=2.6
                    , bytestring ^>=0.12
                    , vector     ^>=0.13
                    , directory  ^>=1.3
                    , filepath   ^>=1.5
                    , time       ^>=1.15
                    , containers ^>=0.8
    default-language: GHC2021
    -- func-spec 0022 S5: '-N' hands the parallel sampler every core the
    -- machine has, and '-rtsopts' lets a player who wants otherwise say so
    -- on the command line. Nothing about the picture changes with the core
    -- count -- law 2 -- only how long a frame takes to compute.
    ghc-options:      -Wall -threaded -O2 -rtsopts "-with-rtsopts=-N"

-- The boundary layer's third consumer (func-spec 0014): an authoring
-- tool that loads and casts a spell file and reports what happened. It
-- opens no window and draws nothing, which is the point -- the same
-- dependency discipline as the demo executable (magic-core is NOT
-- reachable, and neither is a renderer), guarded by test/BoundarySpec.hs.
-- 'aeson' is here because the tool reads the two circle-level opt-in keys
-- (phases, fields) back off Magic.Codec's own canonical encoding.
executable magic-validate
    main-is:          Main.hs
    hs-source-dirs:   tools
    other-modules:    Validate
    build-depends:    base       ^>=4.22
                    , particle-magic:magic-boundary
                    , aeson      ^>=2.3
                    , bytestring ^>=0.12
                    , vector     ^>=0.13
                    , directory  ^>=1.3
    default-language: GHC2021
    -- Same reason as the demo executable (func-spec 0022 S5): the CLI casts
    -- spells, so it samples, so it wants the cores.
    ghc-options:      -Wall -O2 -threaded -rtsopts "-with-rtsopts=-N"

-- The machine-readable half of the authoring surface (func-spec 0024 S1).
-- Prints (or checks) the JSON Schema for a spell file, so an editor's JSON
-- completion and a CI schema step can consume the format without reading
-- Haskell. Narrower than magic-validate on purpose: the schema is a
-- hand-written declaration table (func-spec 0024 §7-5), so this tool casts
-- nothing and does not depend on the library at all -- which is also what
-- makes 'magic-schema --check' a golden comparison rather than a second
-- opinion.
executable magic-schema
    main-is:          SchemaMain.hs
    hs-source-dirs:   tools
    other-modules:    Schema
    build-depends:    base       ^>=4.22
                    , aeson      ^>=2.3
                    , bytestring ^>=0.12
                    , text       ^>=2.1
                    , directory  ^>=1.3
    default-language: GHC2021
    ghc-options:      -Wall -O2

-- The human-readable half (func-spec 0024 S2): what a spell file adds up
-- to, as a report. Every number in it comes from an existing
-- Magic.Interface query (func-spec 0010 §9.3) -- the tool adds no analysis
-- of its own, it only lays the answers out. Same dependency discipline as
-- magic-validate: the boundary layer, no core, no renderer.
executable magic-inspect
    main-is:          InspectMain.hs
    hs-source-dirs:   tools
    -- 'Validate' comes along because Inspect imports its 'defaultContext'
    -- and 'lifetimeOf': the two tools report the same cast, and the way to
    -- guarantee that is to have one definition of it (func-spec 0024 S2).
    other-modules:    Inspect
                    , Validate
    build-depends:    base       ^>=4.22
                    , particle-magic:magic-boundary
                    , aeson      ^>=2.3
                    , bytestring ^>=0.12
                    , vector     ^>=0.13
                    , directory  ^>=1.3
    default-language: GHC2021
    -- Same reason as magic-validate (func-spec 0022 S5): it casts spells.
    ghc-options:      -Wall -O2 -threaded -rtsopts "-with-rtsopts=-N"

-- The second supported consumption mode (ADR-0011, func-spec 0009): the
-- same boundary layer behind a C ABI, so non-Haskell hosts (Unity, Godot,
-- a C/C++ engine) can drive the spell lifecycle and render the six SoA
-- columns themselves. Shell-layer component, same discipline as the
-- executable: magic-core is NOT reachable from here, only the public
-- boundary plus the two marshalling libraries its signatures are written
-- in (bytestring for loadCircle's input, vector for the buffer's columns).
-- Guarded by test/FFIContractSpec.hs.
foreign-library particle-magic-ffi
    type:             native-shared
    if os(windows)
        options:      standalone
        -- Without an explicit export list the DLL links but resolves
        -- nothing for a host; see the .def file's own note.
        mod-def-file: particle-magic-ffi.def
    hs-source-dirs:   src/ffi
    other-modules:    Magic.FFI
                    , Magic.FFI.Registry
    -- pm_init.c is the runtime's lifecycle (state machine, configuration,
    -- shutdown); pm_gate.c is the I3 gate every other exported symbol goes
    -- through, so that calling one before pm_init or after pm_shutdown is
    -- PM_ERR_STATE rather than a dead host process (host-runtime F003).
    c-sources:        cbits/pm_init.c
                    , cbits/pm_gate.c
    include-dirs:     include
    install-includes: particle_magic.h
    build-depends:    base       ^>=4.22
                    , particle-magic:magic-boundary
                    , bytestring ^>=0.12
                    , vector     ^>=0.13
    default-language: GHC2021
    ghc-options:      -Wall -O2 -threaded
    -- host-runtime F007 (design.md C4): two platform-specific link
    -- settings. Neither changes a bit of what the library computes; both
    -- are about how a host's loader finds it once the file has moved off
    -- the machine that built it.
    --
    -- Linux cannot have the literal `options: standalone` Windows gets:
    -- standalone means "link the static way", and the ghcup bindist's
    -- static archives are not PIC, so ld refuses to put them in a shared
    -- object (measured 2026-08-20; adding -fPIC only moves the error to
    -- the next prebuilt archive). The equivalent is to ship the shared
    -- object's whole closure beside it and search $ORIGIN -- the folder
    -- the library itself is in -- which is what this rpath asks for.
    --
    -- It is not sufficient on its own: cabal writes 46 absolute RUNPATH
    -- entries of its own (this developer's cabal store and ~/.ghcup) and
    -- they come first, so on the build machine the loader still prefers
    -- them. That is harmless for a host, whose machine has no such
    -- directories, but it makes "is this drop self-contained?"
    -- unanswerable here. packaging/pack.sh therefore rewrites the packed
    -- copy's RUNPATH down to $ORIGIN alone, and its --verify asserts in a
    -- clean environment that every dependency resolves inside the drop.
    -- (-fno-use-rpaths was tried and measured to change nothing: the
    -- entries are cabal's, not GHC's.)
    if os(linux)
        ghc-options: "-optl-Wl,-rpath,$ORIGIN"
    -- Cabal does not give a foreign library an install_name, so a .dylib
    -- would record its build path and be unloadable once moved; @rpath
    -- makes the host's own rpath decide. NOT VERIFIED this round -- there
    -- is no macOS machine here, so the manifest marks both macOS drops
    -- verified:false (F007 A3, docs/release.md 6.1).
    if os(darwin)
        ghc-options: "-optl-Wl,-install_name,@rpath/libparticle-magic-ffi.dylib"

-- The poison library (host-runtime F006 T9). Field for field the stanza
-- above, plus exactly three differences:
--
--   * test/oop/poison on hs-source-dirs and Magic.FFI.Poison on
--     other-modules -- the one module that is not in src/ffi, which is why
--     FFIContractSpec's three-way reconciliation (it reads
--     src/ffi/Magic/FFI.hs, the shipped .def and the header) needs no
--     change at all;
--   * its own mod-def-file, a mirror of the shipped one plus the extra
--     symbol. test/OopSmokeSpec.hs pins the difference to exactly
--     {pm_poison_spell} so the mirror cannot rust;
--   * buildable: False unless -foop-poison, so this costs nothing in a
--     normal build.
--
-- The product is particle-magic-ffi-poison.dll / libparticle-magic-ffi-
-- poison.so, so packaging/artifacts.json (which names the shipped files
-- literally) cannot pick it up by accident.
foreign-library particle-magic-ffi-poison
    if !flag(oop-poison)
        buildable:    False
    type:             native-shared
    if os(windows)
        options:      standalone
        mod-def-file: test/oop/particle-magic-ffi-poison.def
    hs-source-dirs:   src/ffi, test/oop/poison
    other-modules:    Magic.FFI
                    , Magic.FFI.Registry
                    , Magic.FFI.Poison
    c-sources:        cbits/pm_init.c
                    , cbits/pm_gate.c
    include-dirs:     include
    build-depends:    base       ^>=4.22
                    , particle-magic:magic-boundary
                    , bytestring ^>=0.12
                    , vector     ^>=0.13
    default-language: GHC2021
    ghc-options:      -Wall -O2 -threaded
    if os(linux)
        ghc-options: "-optl-Wl,-rpath,$ORIGIN"

test-suite spec
    type:             exitcode-stdio-1.0
    main-is:          Spec.hs
    -- app/ is included so the loop + interpreters are tested as-built;
    -- the raylib-touching modules (Main, App.Render.Raylib3D) are NOT
    -- listed, keeping the test suite headless and h-raylib-free.
    -- src/ffi is included so the C ABI shell is tested in-process: the
    -- foreign-exported functions are ordinary Haskell functions, called
    -- here through real CStrings and Ptrs (func-spec 0009 §2). Loading the
    -- built shared object is the manual smoke, §8 S6.
    -- tools/ joins them in func-spec 0014 for the same reason app/ is
    -- here: the CLI's pure half is tested as-built. tools/Main.hs (the IO
    -- shell) is not listed, so no second 'Main' module is compiled in.
    hs-source-dirs:   test, app, src/ffi, tools
    other-modules:    BoundarySpec
                    , TypesSpec
                    , CircleSpec
                    , CircleCodecSpec
                    , ShapeSpec
                    , EnvelopeSpec
                    , CompileCoreSpec
                    , CompileFoldSpec
                    , SampleSpec
                    , Acceptance2Spec
                    , BufferSpec
                    , CodecSpec
                    , PipelineSpec
                    , StepSpec
                    , EffectsSpec
                    , HotReloadSpec
                    , AcceptanceSpec
                    , ExprGen
                    , ExprEvalSpec
                    , ExprParseSpec
                    , ExprRenderSpec
                    , ExprGoldenSpec
                    , CompileExprSpec
                    , RuneCodecSpec
                    , SampleExprSpec
                    , Acceptance4Spec
                    , StepObserveSpec
                    , QuadBatchSpec
                    , SceneEffectsSpec
                    , ReloadStatusSpec
                    , HudSpec
                    , SpellSwitchSpec
                    , OptFlagsSpec
                    , Acceptance5Spec
                    , PhasePlanSpec
                    , PhaseCodecSpec
                    , CompileLifecycleSpec
                    , FormationSpec
                    , PhaseSampleSpec
                    , BackCompatSpec
                    , Acceptance6Spec
                    , FFIHarness
                    , FFILifecycleSpec
                    , FFIObserveSpec
                    , FFIErrorSpec
                    , FFIContractSpec
                    , Acceptance9Spec
                    , Magic.FFI
                    , Magic.FFI.Registry
                    , ProjectSpec
                    , FlatQuadSpec
                    , FlatEffectsSpec
                    , ViewToggleSpec
                    , Acceptance8Spec
                    , AnalyticRefactorSpec
                    , FieldAccelSpec
                    , FieldStepSpec
                    , CompileFieldSpec
                    , FieldCodecSpec
                    , FieldPlumbingSpec
                    , FieldStepObserveSpec
                    , FieldRebirthSpec
                    , Acceptance7Spec
                    , PerfGoldenSpec
                    , SampleFillSpec
                    , CullSpec
                    , FieldSoASpec
                    , DepthSortSpec
                    , ExprFoldSpec
                    , BudgetPlanSpec
                    , Acceptance10Spec
                    , ColumnsSpec
                    , FFIProjectSpec
                    , BindingContractSpec
                    , Acceptance11Spec
                    , CameraSpec
                    , OrderSpec
                    , FlatCameraSpec
                    , DepthTintSpec
                    , Acceptance13Spec
                    , CapacitySpec
                    , ComposeSpec
                    , ComposeBudgetSpec
                    , SceneSpec
                    , Acceptance12Spec
                    , Validate
                    , ValidateSpec
                    , Schema
                    , JsonSchemaSpec
                    , Inspect
                    , InspectSpec
                    , ValidateJsonSpec
                    , SchemaDocSpec
                    , RescanSpec
                    , Acceptance14Spec
                    , BatchSplitSpec
                    , ShapeVocabSpec
                    , SpriteSpec
                    , Acceptance15Spec
                    , SigilGen
                    , SigilHashSpec
                    , SigilStrokeSpec
                    , SigilPlanSpec
                    , SigilWiringSpec
                    , Acceptance16Spec
                    , PersistEnvelopeSpec
                    , PersistWiringSpec
                    , Acceptance17Spec
                    , FFISceneSpec
                    , FFISceneCastSpec
                    , Acceptance18Spec
                    , GoldenPlatform
                    , CIWorkflowSpec
                    , ReleaseMetaSpec
                    , ReleaseDocSpec
                    , SigilSpinSpec
                    , SigilMotionSpec
                    , SigilMotionWiringSpec
                    , Acceptance20Spec
                    , ElementVocabSpec
                    , FaceShapeVocabSpec
                    , TrajectoryVocabSpec
                    , FieldVocabSpec
                    , VocabCodecSpec
                    , FlatReadabilitySpec
                    , Acceptance21Spec
                    , SpaceExamples
                    , SpaceBoundsSpec
                    , OccupancySpec
                    , AnchorCodecSpec
                    , MultiAnchorSpec
                    , SpaceInterfaceSpec
                    , FFISpaceSpec
                    , Acceptance25Spec
                    , ExprCodeSpec
                    , ExprCseSpec
                    , ExprCodeWiringSpec
                    , ParallelSampleSpec
                    , ExampleHostSpec
                    , BufferVelocitySpec
                    , VelocitySampleSpec
                    , TrailVocabSpec
                    , FFIObserveExSpec
                    , ShaderPipelineSpec
                    , TrailQuadSpec
                    , BloomSpec
                    , SoftParticleSpec
                    , CrossBatchOrderSpec
                    , Acceptance23Spec
                    , App.Effects
                    , App.HotReload
                    , App.Loop
                    , App.TestInterp
                    , App.Camera
                    , App.Render.Quads
                    , App.Render.Flat
                    , App.Render.Order
                    , App.Render.Chunk
                    , App.Render.Sprite
                    , App.Render.Shader
                    , App.Render.Post
                    , App.Scene
                    , App.Hud
                    , App.Panel
                    , ParamPanelSpec
                    , PanelWriteBackSpec
                    , Acceptance24Spec
                    , FFIHandleSpec
                    , FFIFirewallSpec
                    , FFIRuntimeSpec
                    , FFIRuntimeCapsSpec
                    , FFIStepPlanSpec
                    , FFIAdvanceGuardSpec
                    , FFIThreadSpec
                    , PackagingSpec
                    , OopSmokeSpec
                    , ExampleLoopSpec
                    , DocContradictionSpec
                    , SigilTimingSpec
                    , SigilTimingCodecSpec
                    , SigilLingerSpec
                    , SigilHoldSpec
                    , SigilTimingInvariantSpec
                    , SigilVolumeSpec
                    , SigilVolumeCodecSpec
                    , SigilVolumeStackSpec
                    , SigilVolumeInvariantSpec
                    , SigilVolumeStructureSpec
                    , SigilVolumeBoundsSpec
                    , ManaWeightSpec
                    , ManaCapSpec
                    , ManaErrorRenderSpec
                    , ManaCapZeroRippleSpec
                    , IntegrationContractSpec
    -- host-runtime F003: the same two C translation units the foreign
    -- library is built from, so FFIRuntimeSpec drives the real state
    -- machine and the real gate in process rather than a copy of them.
    c-sources:        cbits/pm_init.c
                    , cbits/pm_gate.c
    include-dirs:     include
    build-depends:    base       ^>=4.22
                    , particle-magic:magic-core
                    , particle-magic:magic-boundary
                    , hspec      ^>=2.11
                    , QuickCheck ^>=2.18
                    , aeson      ^>=2.3
                    , bytestring ^>=0.12
                    , vector     ^>=0.13
                    , effectful  ^>=2.6
                    , directory  ^>=1.3
                    , filepath   ^>=1.5
                    , time       ^>=1.15
                    , containers ^>=0.8
                    , text       ^>=2.1
    build-tool-depends: hspec-discover:hspec-discover ^>=2.11
    default-language: GHC2021
    -- func-spec 0022 S5: '-N' so ParallelSampleSpec runs the parallel
    -- sampler on every core this machine has (and can vary the count with
    -- setNumCapabilities); '-T' so ExprCodeSpec can read allocation
    -- counters and assert that bytecode evaluation allocates nothing per
    -- instruction.
    ghc-options:      -Wall -threaded -rtsopts "-with-rtsopts=-N -T"

-- Baseline measurements for the future throughput spec (func-spec 0005
-- §8 S8). A separate stanza so it is outside the library/executable
-- whitelist BoundarySpec guards, and h-raylib-free: all it measures is
-- pure code.
benchmark bench
    type:             exitcode-stdio-1.0
    main-is:          Bench.hs
    hs-source-dirs:   bench, app
    other-modules:    App.Render.Quads
    -- magic-core is reachable from here (and only from here): func-spec
    -- 0010 S8 measures the 10k-100k range, which is past 'budgetCap', so
    -- the synthetic fixtures are built as 'CompiledSpell' values directly
    -- instead of going through 'compile'. The sampler itself never
    -- consults the cap, so this measures the real code path -- and the
    -- benchmark stanza is outside the dependency whitelist BoundarySpec
    -- guards precisely so a measuring instrument can reach further than a
    -- shipped component may.
    build-depends:    base        ^>=4.22
                    , particle-magic:magic-core
                    , particle-magic:magic-boundary
                    , tasty-bench ^>=0.5
                    , bytestring  ^>=0.12
                    , vector      ^>=0.13
    default-language: GHC2021
    -- func-spec 0022 S6: the parallel speed-up cannot be measured without
    -- '-threaded', and '-rtsopts' is what lets the acceptance record be
    -- taken at '-N1', '-N2', '-N4', '-N8' from one built binary.
    ghc-options:      -Wall -O2 -threaded -rtsopts "-with-rtsopts=-N"
